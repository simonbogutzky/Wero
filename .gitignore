# Aider runtime files (don't commit these)
.aider.input.history
.aider.chat.history.md
.aider.tags.cache.v4/

# Only commit configuration files:
# .aider.conf.yml
# .aider.model.settings.yml
/.scannerwork
/build
